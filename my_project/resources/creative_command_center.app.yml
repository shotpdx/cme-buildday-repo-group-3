resources:
  apps:
    creative_command_center:
      name: ${var.app_name}
      description: Self-contained Creative Command Center demo with a CSV-backed FastAPI backend and React frontend.
      source_code_path: ..
